//Function named navigateTo that takes a URL as a parameter
function navigateTo(url) {
    //window.open() method opens the specified URL in a new browser window or tab
    //The '_blank' parameter indicates that the URL should be opened in a new window or tab
    window.open(url, '_blank');
}
